# CH101 공통 베이스 메시 WIP 리뷰 — 2026-08-16

상태: `REJECTED AS VISUAL / WIP ONLY / GATE A·B BLOCKED`

## 이번 실행

- 스크립트: `scripts/blender/build_ch101_base_mesh_wip.py`
- 산출물: `artifacts/CH101_common_base_run3/CH101_CommonBase_AB_WIP_v001.blend`
- 렌더: `artifacts/CH101_common_base_run3/renders/CH101_CommonBase_AB_WIP_front.png`
- 검사: Blender 4.5 백그라운드 실행 성공, Python 구문 검사 성공

## 판정

이번 실행은 이전 단일 프리미티브 스캐폴드보다 연속 링 메시, 얼굴 타원형, 눈 홍채/동공,
곡면 헤어 스트랜드를 추가했지만, Gate 기준을 통과하지 못한다. 현재 결과는 여전히
토이/레고형 파츠 조합으로 읽히며, 아래 항목이 즉시 실패 조건에 해당한다.

- 얼굴이 실제 애니메이션 얼굴 평면이 아니라 단순한 둥근 메시와 돌출 파츠로 보임
- 흉부·허리·골반·허벅지의 성인 여성 실루엣이 단순 원통으로 남아 있음
- 재킷 패널·쇼츠·부츠가 의상 패턴/재질이 아니라 둥근 박스 부품으로 보임
- 손·발·관절·헤어가 제작용 토폴로지로 연결되지 않았고, A/B 턴어라운드도 없음
- 3D LookDev WIP v004의 프리미엄 애니 3D 목표와 시각적 격차가 큼

따라서 이 파일은 Re:Camp에 게임 자산으로 복사하거나 Gate B 후보로 표시하지 않는다.
기존 v007~v010 리그/예산 스캐폴드와 동일하게 기술 실험 산출물로만 보관한다.

## run4 재검토

run4는 동일 스크립트에 세로 비율 1.15 보정, 패널형 재킷 메시, 눈/헤어·부츠 보강을
적용해 `artifacts/CH101_common_base_run4/`에 다시 렌더했다. 5.3~5.4등신에 가까워졌지만
얼굴·손·발·의상이 여전히 분리된 장난감형 파츠로 보이므로 시각 반려 상태는 유지한다.
자동 프리미티브 개선만으로 프리미엄 목표에 도달했다고 판정하지 않는다.

## 연결 베이스 run2 재검토

`build_ch101_connected_base_wip.py`로 Skin Modifier 기반의 연결 몸통·팔·다리와 별도
의상/헤어/장비를 생성해 `artifacts/CH101_connected_base_run2/`에 렌더했다. 몸통의
연결성은 개선됐지만 얼굴·손·발·의상 패턴이 여전히 장난감형이고, 피부·의상·장비가
실제 게임 모델의 표면 품질로 읽히지 않아 반려한다. 이 실행도 Gate B나 Unity 입력으로
승격하지 않는다.

## voxel body run1 재검토

겹치는 인체 볼륨을 하나로 합친 뒤 voxel remesh한 `build_ch101_voxel_body_wip.py`를
실행해 `artifacts/CH101_voxel_body_run1/`에 렌더했다. 몸통 연결성은 이전 패스보다
나아졌지만, 재킷·쇼츠·손·발·얼굴이 여전히 단순 파츠로 읽히며 프리미엄 3D 목표와
차이가 크다. 따라서 이 패스도 최종 자산·Gate B·Unity 입력으로 승격하지 않는다.

자동 프리미티브/voxel 반복만으로는 품질 기준을 충족하지 못한다는 결론을 기록하고,
다음 제작은 실제 고해상도 수동/스컬프 메시를 `CH101_A_HighRes_Production_v001.blend`
로 인입하는 단계로 고정한다.

## 다음 제작 단계

1. 외부/수동 모델링이 가능한 실제 고해상도 베이스(머리·손·발·가슴·골반)를 먼저 확보한다.
2. CH101 A 변형을 기준으로 의상 패턴과 장비를 분리하고, 3/4·측면·후면에서 실루엣을 검증한다.
3. 실제 메시가 확보되기 전에는 리그·Unity 임포트·Gate B를 진행하지 않는다.
4. 새 모델이 `ART_REVIEW_CHECKLIST.md`의 CHA-B01~B10 중 시각 항목을 충족한 뒤에만
   UV/리그/LOD 및 Unity 연동으로 이동한다.

## 승인 상태

사용자 승인 없음. `APPROVED` 표기 금지.
